import 'package:flutter/material.dart';

class ParticipationMeetingCard extends StatelessWidget {
  const ParticipationMeetingCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
